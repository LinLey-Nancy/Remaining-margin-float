# Remaining Margin Float v1.8.7

## 历史采样与续接

- 自动刷新调整为每 60 秒一次；每次成功的自动刷新都会追加一个趋势样本，
  连续使用 1 小时通常会得到约 60 个样本。
- 用户通过按钮、托盘菜单或快捷键主动刷新时，也会立即追加独立样本，不再被
  临近的自动刷新覆盖。
- 近 7 日数据继续保存在当前用户的本地数据目录，跨程序重启和覆盖升级续接；
  日常采样改用增量追加，跨日自动压缩并清理过期记录。
- 完整页面状态同步按 60 秒保存；相同内容共享数据，只保留独立采集时间节点。

## 24 小时与近 7 日趋势

- 24 小时和近 7 日均使用从当前时刻向前滚动的真实时间窗口，并补入窗口边界前
  最近的样本，避免昨天同期或七日前边界附近的变化在折线中消失。
- 折线横坐标按真实采样时间计算，不再因样本稀疏而等距铺开；卡片样本数仅统计
  窗口内实际样本。
- 检测到当前余量高于历史最新余量时，将其视为新的统计基准；趋势从最后一次
  上涨后的区段重新开始，不再显示跨重置的“增加 Npp”或低到高比较。
- 重置后尚无第二个样本时显示“积累中 / 等待更多样本”，随后只统计新基准后的
  正常消耗。

## 详情界面

- 展开详情后，顶部余量百分比放大并左对齐，提升信息层级。
- 下次预计重置时间保持右侧位置并适度放大，字号仍明显小于主余量。
- 收起窗口时恢复原有居中紧凑布局。

## 稳定性与兼容

- 包含 v1.8.6 之后对计时准确性与新判定逻辑的修复。
- 新增一分钟连续采样、手动刷新、多次重置、滚动边界、真实时间轴和详情字号
  层级回归；完整诊断覆盖数据、刷新协调、视觉转换、状态恢复和更新流程。
- 保持 Windows PowerShell 5.1、既有设置、历史记录、提醒阈值、同一安装 AppId
  和自动更新兼容，可直接覆盖升级 v1.8.6。

## 下载

下载 `Remaining-Margin-Float-v1.8.7-Setup.exe` 后运行安装向导，并可使用同名
`.sha256` 文件核对完整性。已启用自动更新的安装版，也可在网络条件合适时自动
完成下载、校验、覆盖安装和重启。

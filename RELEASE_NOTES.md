# Remaining Margin Float v1.4.1

## 刷新可靠性

- 限制本地 Codex 会话日志的读取范围，不再因扫描大型历史文件阻塞悬浮窗。
- 使用绝对截止时间计算自动刷新倒计时，修复立即刷新后倒计时停住或卡在 0 秒的问题。
- 官方 Codex 用量请求增加超时、瞬时错误重试和 `Retry-After` 支持。
- 刷新失败时保留最近一次有效数据，避免紧凑布局退化为“读取失败”。

## 贴边隐藏能量条

- 将原有低可见度温度计改为高亮竖向能量条，以绿、黄、橙、红表达余量阈值。
- 使用 12px 可视能量条与 14px 悬停热区，四边增加完整描线，内部色块统一缩进
  1px，四角使用 2px 微圆角。
- 按当前显示器的物理像素校准隐藏和展开位置，修复多次展开后与屏幕边缘间隙漂移
  的问题。
- 隐藏态以能量条外缘校准，展开态以紧凑卡片外缘校准，修复悬停时卡片闪现后再次
  滑出屏幕的问题。

## 验证

- 新增左右贴边、即时与动画循环、悬停计时器、四边描线和色块约束回归测试。
- 发布测试继续覆盖组件合并、真实 WPF 回调、刷新计时器、已有实例唤醒、缺失或
  篡改脚本拒绝、启动入口选择和最终 ZIP 内容。

## 下载

下载 `Remaining-Margin-Float-v1.4.1.zip`，解压后保持目录内容完整，并运行
`RemainingMarginFloat.exe`。可使用同名 `.sha256` 文件核对下载完整性。

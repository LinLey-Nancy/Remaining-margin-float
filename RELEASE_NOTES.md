# Remaining Margin Float v1.4.0

## 趋势与提醒

- 在详情中显示最近 24 小时和 7 天的余量趋势。
- 根据最近下降速度预测预计耗尽时间；Codex 预测会同时考虑额度重置时间。
- 余量首次降到 20% 或以下时发送通知区域提醒，并可从窗口或托盘菜单关闭。
- DeepSeek 未设置预算基准时继续只显示余额趋势，不触发百分比低余量提醒。

## 隐私与可靠性

- 趋势历史只在本机保存脱敏样本，按约 5 分钟聚合并仅保留最近 8 天。
- 历史文件不包含账号名称、邮箱、Token、API Key、访问令牌或原始会话内容。
- 检测到额度重置或余额增加后，从新的下降区间重新计算耗尽预测。
- 修复发布版 WPF 回调在线程和重入场景下可能失效的问题，并增加实际图形回调测试。
- 修复打包版在已有后台实例时再次双击 EXE 会误报启动失败的问题；现在会唤醒
  已有窗口并正常退出第二个启动器。
- 修复打包版从菜单打开 DeepSeek 设置后，保存或取消可能因模态回调重入而卡住
  的问题。

## 源码与发布

- 将单文件源码按 App、Core、Provider、Infrastructure、Diagnostics 和 UI 拆分，
  并把主窗口布局迁移到独立 XAML，便于维护和审查。
- 构建时仍自动合并为单个可审查的 PowerShell 脚本，并在透明便携包中附带
  MIT 许可证和隐私说明。
- 发布测试会核对组件清单、合并结果、启动器哈希验证和最终 ZIP 内容。

## 下载

下载 `Remaining-Margin-Float-v1.4.0.zip`，解压后保持目录内容完整，并运行
`RemainingMarginFloat.exe`。可使用同名 `.sha256` 文件核对下载完整性。

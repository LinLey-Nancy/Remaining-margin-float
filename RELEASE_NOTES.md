# Remaining Margin Float v1.7.0

## 自定义快速下降提醒

- 新增独立的短时间快速下降监控，可分别设置启用状态、5–1440 分钟时间范围和下降阈值。
- Codex 按百分比点判断；DeepSeek 可选择百分比点或具体金额。
- DeepSeek 使用百分比模式时需要预算基准；金额模式直接基于官方余额，不依赖预算。
- 快速下降通知按数据源、指标和币种去重；窗口内下降量回落到阈值以下后才允许再次触发，且不会与同次低余量通知重复弹出。
- 所有提醒开关、时间窗口、模式和阈值写入本机设置，重启、更新版本后继续生效。

## 24 小时与近 7 日趋势

- 趋势从文字摘要升级为并排曲线卡片，显示方向、变化量、起止值和有效样本数。
- 横轴按完整 24 小时或 7 日窗口对齐，避免样本稀疏时把短时间变化误画成完整周期。
- 曲线按区间保留高低极值后降采样，数据量增长时仍能保留峰谷并控制渲染成本。
- DeepSeek 设置预算后同时保存百分比和余额历史，因此切换快速下降规则时无需重新积累全部记录。

## 布局与可读性

- 详情窗口调整为 400×560，强化账号、主指标、趋势和状态的视觉层级。
- 合并四个指标为统一信息面板，减少重复边框；趋势和刷新信息使用更清晰的字号、对比度和间距。
- 新增快速下降状态条，直接显示当前窗口内下降量、阈值或仍需积累样本的原因。
- 保留 80×80 紧凑正方形、贴边能量条、多显示器边界处理和减少动画支持。

## 验证与发布

- 新增 Codex 百分比、DeepSeek 百分比/金额、预算依赖、自定义时间窗口、双指标历史和设置界面绑定回归。
- 修复历史采样、颜色插值和金额下降计算可能因数值重载被取整的问题，小数百分比与金额阈值现在按实际精度判断。
- 发布包只包含启动器、合并脚本、说明、许可证和隐私文件，不包含测试、截图或仓库维护脚本。
- GitHub Release 标题继续只显示版本标签 `v1.7.0`。
- 继续支持 Windows PowerShell 5.1、WPF、60 秒刷新周期和零运行时第三方依赖。

## 下载

下载 `Remaining-Margin-Float-v1.7.0.zip`，解压后保持目录内容完整，并运行
`RemainingMarginFloat.exe`。可使用同名 `.sha256` 文件核对下载完整性。

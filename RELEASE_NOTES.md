# Remaining Margin Float v1.9.0

## Codex 5 小时与每周双周期额度

- 同时读取 Codex 的 5 小时额度与每周额度，并按照 300 分钟和 10080 分钟的
  实际窗口时长识别，不依赖接口把它们放在 primary 还是 secondary。
- 根据检测到的套餐自动选择主额度：Plus 使用 5 小时额度，100/200 美元的 Pro
  档（`prolite` / `pro`）使用每周额度。
- 主额度统一只在顶部展示一次：Plus 顶部显示 5 小时额度，并以 48px 单行信息条
  补充每周额度，详细窗收短为 522px；Pro 顶部显示每周额度并隐藏重复额度卡，
  详细窗随内容收短为 474px。补充条隐藏重复的已用百分比，但在无障碍描述中保留。
- 顶部主值、重置时间和周期标签采用约束式网格布局；主额度进度条横跨内容区，
  周期标签与重置时间紧贴在进度条上方，长倒计时会省略显示且不与左侧标签重叠。
- 今日 Token 与今日缓存收进浅色统计带，使用细边界、9px 圆角和低对比度中轴分隔，
  在不增加高度的情况下与上下额度、趋势区域形成清晰层级。

## 缺失数据与兼容行为

- Plus 的 5 小时窗口缺失时明确显示“未知”，即使每周窗口仍然有效，也不会把
  每周数据冒充 5 小时数据；Pro 则直接以每周窗口作为主额度。
- 官方接口与本地会话快照都支持双周期解析；旧的单窗口数据仍可读取，并按套餐
  判断它属于当前主额度后再参与展示与监控。
- DeepSeek 的余额、预算百分比、Token 汇总和提醒逻辑保持不变。

## 趋势与提醒跟随套餐主额度

- Codex 的 24 小时、近 7 日趋势、耗尽预测、低额度提醒和快速下降提醒使用当前
  套餐主额度。
- 新趋势样本带有 `FiveHour` 或 `Weekly` 周期标识，不同周期不会互相混入，避免
  套餐切换后出现曲线跳变或快速下降误报。
- 通知标题和状态文案会显示当前是“5 小时”还是“每周”额度。

## 下载

正式发布后下载 `Remaining-Margin-Float-v1.9.0-Setup.exe` 并运行安装向导，可用
同名 `.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

# Remaining Margin Float v1.5.0

## 自定义低余量提醒

- 悬浮窗和通知区域图标的右键菜单新增“提醒阈值…”，可输入 1–99 的整数百分比。
- 默认阈值继续保持 20%；保存后立即应用于 Codex 与 DeepSeek，并在两个菜单中同步显示当前值。
- 提醒开关和阈值写入现有本机设置，重启后自动恢复；非法旧值会安全回退到 20%。

## DeepSeek 热刷新优化

- Claude Code 日志清单未变化时直接复用今日、本月和最近消息的聚合结果，避免每 60 秒重复去重和计费汇总。
- 日志新增、删除、追加或跨日后自动使聚合缓存失效，继续使用逐文件缓存只重读变化内容。
- 在本机 134 个日志文件、约 91 MB 数据的多轮基准中，热读取由 v1.4.3 的约 1139.54 ms 降至约 92–146 ms，减少约 87–92%；实际耗时会随磁盘和日志数量变化。

## 验证与兼容性

- 新增固定日志回归，验证聚合缓存命中后数据一致，日志追加后会重新统计。
- 新增阈值范围、穿越判断、菜单同步、设置快照和 WPF 对话框结构诊断。
- 继续支持 Windows PowerShell 5.1、WPF、60 秒刷新周期和零运行时第三方依赖。
- 发布包仍为未签名的透明便携包。

## 下载

下载 `Remaining-Margin-Float-v1.5.0.zip`，解压后保持目录内容完整，并运行
`RemainingMarginFloat.exe`。可使用同名 `.sha256` 文件核对下载完整性。

# Remaining Margin Float v1.3.0

## 安全与发布改进

- 发布物改为透明 ZIP，包含启动器、可审查的 PowerShell 脚本和使用说明。
- 启动器在运行前验证脚本 SHA-256，不再把 Base64 脚本嵌入 EXE 或释放到用户目录。
- 启动器在自身进程中托管 PowerShell，不再创建隐藏的 `powershell.exe` 子进程。
- 修复 1.3.0 测试包启动时因 UTF-8 BOM 导致“赋值表达式无效”的错误。
- 修复仓库 `Start-RemainingMarginFloat.cmd` 忽略打包版、启动后看似无响应以及
  子进程继承输出句柄的问题。
- 单实例 Mutex 和唤醒事件按 Windows 用户 SID 隔离，避免其他用户、沙箱或
  服务会话中的后台实例导致双击 EXE 后静默退出。
- 删除运行时和开机启动命令中的 `ExecutionPolicy Bypass`。
- Codex 官方接口访问默认关闭；只有用户明确确认后才读取 `.codex\auth.json`。
- 开机启动继续保持默认关闭，只在用户主动选择后创建。
- 已启用开机启动的旧版本会迁移到经过哈希验证、成对更新的 EXE 与脚本目录。
- 修复贴边隐藏后，鼠标位于进度条与屏幕边缘之间时无法唤醒紧凑布局的问题。
- 修复反复唤醒和收回紧凑布局后，贴边进度条与屏幕边界间距变化的问题。
- 发布流水线增加高风险行为检查、归档验证和校验和生成流程。
- 发布测试覆盖完整图形初始化、启动脚本选择、缺失/篡改脚本拒绝及最终 ZIP
  内容与哈希。

## 下载

下载 `Remaining-Margin-Float-v1.3.0.zip`，解压后保持目录内容完整，并运行
`RemainingMarginFloat.exe`。可使用同名 `.sha256` 文件核对下载完整性。

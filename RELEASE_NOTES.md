# Remaining Margin Float v1.8.2

## 可选自动更新

- 悬浮窗与托盘右键菜单新增“软件更新 > 自动更新并重启（仅合适网络）”开关，默认关闭。
- 用户明确启用后，应用会在发现新版本时自动下载、校验、静默覆盖安装，并在成功后重新启动。
- 自动更新仅支持安装版；源码或便携运行继续使用手动检查、下载和安装流程。
- 启用时会明确提示当前发布物尚未进行可信代码签名，并由用户决定是否接受该风险。

## 网络与安装安全

- 仅当 Windows 报告当前连接可访问互联网、非按流量计费、非漫游、未接近或超过流量限制且后台流量未受限时执行自动更新。
- 网络条件不合适时不会下载或安装，应用会保留待更新版本，并在网络条件恢复后重试。
- 安装程序仍须通过本仓库 GitHub Release 路径、资产命名、文件大小、SHA-256 和内嵌版本校验。
- 静默更新沿用现有安装位置和组件选择，不传入新的安装目录，不强制结束进程，也不允许安装器自行重启 Windows。
- 更新完成后的应用重启由安装器专用参数和受控 `[Run]` 条目完成。

## 验证与兼容

- 新增非计费网络、计费网络、源码运行模式、自动下载意图与静默安装参数诊断。
- 安装器测试新增真实的静默覆盖升级和应用自动重启验证。
- 保持 Windows PowerShell 5.1、现有设置、历史记录、提醒规则和安装目录兼容。
- 安装程序继续使用相同 AppId，可直接覆盖升级 v1.8.1。

## 下载

下载 `Remaining-Margin-Float-v1.8.2-Setup.exe` 后运行安装向导。可使用同名
`.sha256` 文件核对完整性。v1.8.1 尚不具备自动更新开关，需要手动安装
v1.8.2 一次；启用后可从后续版本开始使用自动更新。

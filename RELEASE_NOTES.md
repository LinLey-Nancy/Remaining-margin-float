# Remaining Margin Float v1.8.4

## 更新检查限流修复

- 修复点击“检查更新”时可能出现 `403 (rate limit exceeded)` 的问题。
- GitHub 匿名 REST API 遇到 403 或 429 后，自动改用公共 Release 页面重定向
  获取最新正式版本，不要求用户登录 GitHub，也不需要配置 Token。
- 回退过程不会显示底层 `GetResult` 异常，正常继续完成版本比较和更新流程。

## 提醒设置同步修复

- 修复自定义快速下降时间范围或阈值后，详情提示仍显示旧设置的问题。
- 保存提醒设置后立即按当前连续使用样本重算提示，无需等待下一次自动刷新，
  且不会额外写入历史样本或触发通知。

## 下载安全

- 仅接受本仓库严格匹配的 HTTPS Release 标签与资产地址。
- 回退模式先核对安装程序及 `.sha256` 的响应大小，再执行下载。
- 下载完成后仍验证文件大小、SHA-256 和安装程序内嵌版本；自动安装仍遵守
  合适网络、安装版运行和用户已明确启用三个条件。

## 验证与兼容

- 新增 API 限流回退、安装资产选择和恶意 Release 重定向拒绝诊断。
- 保持 Windows PowerShell 5.1、现有设置、历史记录、提醒规则和自动更新开关兼容。
- 安装程序继续使用相同 AppId，可直接覆盖升级 v1.8.3。

## 下载

下载 `Remaining-Margin-Float-v1.8.4-Setup.exe` 后运行安装向导，并可使用同名
`.sha256` 文件核对完整性。已在 v1.8.2 或 v1.8.3 启用自动更新的安装版，
也可在网络条件合适时自动完成下载、校验、覆盖安装和重启。

# Remaining Margin Float v1.8.0

## 版本更新检测

- 应用启动约 8 秒后在后台检查 GitHub 最新正式 Release，运行期间最多每 6 小时检查一次，不阻塞余量刷新和界面交互。
- 托盘菜单新增“检查更新…”，用户可以随时手动检查；没有新版本时会明确提示当前已是最新版。
- 检测到更高的三段语义版本后先询问用户，不会静默下载或自动安装。
- 更新资产必须使用 `Remaining-Margin-Float-vX.Y.Z-Setup.exe` 固定命名，并来自本仓库对应版本的 GitHub HTTPS Release 路径。
- 用户同意下载后，同时取得安装程序与 SHA-256；文件名、来源、哈希和内嵌版本必须全部匹配。
- 当前安装程序尚未配置可信代码签名，应用不会自动执行下载文件，只会按用户选择打开文件所在位置供手动确认；SHA-256 只作为完整性校验，不作为发布者身份证明。
- 自动检查只发送应用名称与版本形式的 User-Agent，不上传账户、使用记录、凭据或诊断数据；网络失败时自动检查保持静默。

## 标准 Windows 安装程序

- GitHub Release 的主要交付物从 ZIP 改为 `Remaining-Margin-Float-v1.8.0-Setup.exe` 与同名 `.sha256`。
- 安装向导支持选择安装目录，默认安装到当前用户的本地应用目录，无需管理员权限。
- 自动创建开始菜单入口，可选创建桌面快捷方式，并提供标准卸载程序；卸载时也会清理应用创建的三种开机启动入口。
- 相同 AppId 支持后续版本覆盖升级；安装目录写入当前用户注册表后，开机启动使用稳定安装路径。
- 发布工作流从 Inno Setup 官方不可变 Release 获取编译器，验证其有效 Authenticode 签名和发布者后才构建安装程序。

## 验证与发布

- 新增语义版本比较、正式 Release 解析、固定资产选择、可信下载路径与校验文件解析诊断。
- Windows CI 和标签发布都会真实执行静默安装、检查五个应用文件与卸载入口，再执行静默卸载并确认文件和注册信息清除。
- 安装包内部只包含启动器、合并脚本、说明、许可证和隐私文件，不包含测试、截图或仓库维护脚本。
- GitHub Release 标题继续只显示版本标签 `v1.8.0`。
- 保持 Windows PowerShell 5.1、WPF、80×80 紧凑布局、使用历史和提醒设置兼容。

## 下载

下载 `Remaining-Margin-Float-v1.8.0-Setup.exe` 后运行安装向导。可使用同名
`.sha256` 文件手动核对下载完整性；应用内更新会自动核对 SHA-256 和内嵌版本。

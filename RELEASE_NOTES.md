# Remaining Margin Float v1.8.8

## 24 小时与近 7 日趋势修复

- 24 小时趋势现在始终展示从当前时刻向前滚动的完整 24 小时记录，近 7 日趋势
  同样展示完整七日窗口，不再因为额度补充或重置而丢失更早的工作趋势。
- 检测到余量上升时，会在折线中断开新旧额度周期，避免把额度重置误画成一次
  用量骤增；重置前后的历史仍会保留在同一时间窗口中。
- 趋势横坐标继续按照真实采样时间绘制，24 小时和近 7 日不会因为样本数量相同
  而显示成不对应实际时间的相同曲线。

## 统计口径修复

- 当前余量高于上一条历史记录时，以当前值作为新的统计基准，只计算新额度周期
  之后的下降量，不再显示“增加 Npp”。
- 卡片中的变化值、起止范围和“积累中”状态统一使用最新额度周期；重置前数据只
  用于完整折线展示，不会混入当前周期统计。
- 范围文字继续按“较高余量 → 较低余量”的正常消耗方向展示，避免低到高的错误
  语义。

## 旧历史样本恢复

- 启动时会从本机已保存的加密全量状态历史中自动补录旧版本遗漏的分钟趋势样本，
  尽量恢复升级前连续工作期间的 24 小时与近 7 日记录。
- 补录按 Provider、指标和采样时间去重，并使用增量游标；重复启动不会重复写入，
  已有趋势样本也不会被覆盖。
- 如果个别历史状态损坏或暂时无法解密，程序会保留原文件并继续启动，不会因补录
  失败阻塞正常使用。

## 采样与兼容性

- 每 60 秒自动刷新成功后继续追加一个样本；按钮、托盘菜单或快捷键触发的主动
  刷新成功后也会作为独立样本记录。
- 近 7 日数据继续保存在当前 Windows 用户的本地数据目录，可跨程序重启和覆盖
  升级续接。
- 保持 Windows PowerShell 5.1、既有设置、提醒阈值、同一安装 AppId 和自动更新
  兼容，可直接覆盖升级 v1.8.7。

## 下载

下载 `Remaining-Margin-Float-v1.8.8-Setup.exe` 后运行安装向导，并可使用同名
`.sha256` 文件核对完整性。已启用自动更新的安装版，也可在网络条件合适时自动
完成下载、校验、覆盖安装和重启。

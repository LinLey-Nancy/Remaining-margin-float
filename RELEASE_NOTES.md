# Remaining Margin Float v1.8.3

## 详情界面

- 在详情界面底部新增当前应用版本信息，便于确认正在运行的版本。
- 版本号与自动刷新倒计时采用同一行的轻量布局，不占用趋势图和通知区域空间。
- 版本显示直接读取应用运行时版本，确保与安装包和发布标签保持一致。

## 验证与兼容

- 新增详情版本标签诊断，验证界面显示与 `VERSION` 一致；同时避免高 DPI
  布局舍入导致贴边轨道诊断误报。
- 保持 Windows PowerShell 5.1、自动更新、现有设置、历史记录和提醒规则兼容。
- 安装程序继续使用相同 AppId，可直接覆盖升级 v1.8.2。

## 下载

下载 `Remaining-Margin-Float-v1.8.3-Setup.exe` 后运行安装向导，并可使用同名
`.sha256` 文件核对完整性。已在 v1.8.2 启用自动更新的安装版，也可在网络条件
合适时自动完成下载、校验、覆盖安装和重启。

# Remaining Margin Float v1.8.5

## 多显示器与贴边稳定性

- 修复不同 DPI 缩放、负坐标副屏和任务栏工作区变化后，贴边窗口可能偏离屏幕
  边缘或使用错误工作区的问题。
- 贴边状态会定期核对当前显示器环境，并在窗口位置变化或恢复显示时重新锚定。
- 贴边能量条的已使用部分改为稳定的不透明深灰，不再使用半透明淡化效果；
  无数据和高对比度模式也使用清晰的灰色状态。

## 数据新鲜度与刷新恢复

- 接口失败并显示上次成功数据时，详情会明确标出采样年龄、回退状态和失败原因。
- 延迟或过期的成功快照会在数据来源与采样时间中标记，便于区分当前数据与旧数据。
- 展示用回退快照不写入历史、不参与快速下降判断，也不会触发额外提醒。
- Codex 与 DeepSeek 对超时、限流和服务端暂时错误使用一致的有界指数退避，
  并尊重服务端 `Retry-After`。

## 详情布局

- 修复详情底部版本号与“版本”、分隔符、自动刷新倒计时没有对齐的问题。
- 中文状态文字继续使用 `Microsoft YaHei UI`，版本号继续使用
  `Segoe UI Variable Text`，现在由同一文本行按共同基线排版。

## 验证与兼容

- 新增 125% / 150% DPI、负坐标显示器、任务栏工作区变化、贴边重新锚定、
  不透明已用区域、多字体基线和回退快照隔离诊断。
- 保持 Windows PowerShell 5.1、现有设置、历史记录、提醒规则和自动更新开关兼容。
- 安装程序继续使用相同 AppId，可直接覆盖升级 v1.8.4。

## 下载

下载 `Remaining-Margin-Float-v1.8.5-Setup.exe` 后运行安装向导，并可使用同名
`.sha256` 文件核对完整性。已启用自动更新的安装版，也可在网络条件合适时自动
完成下载、校验、覆盖安装和重启。

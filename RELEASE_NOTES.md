# Remaining Margin Float v1.2.0

## 本次更新

- Codex 余量改为双通道获取：官方用量接口可用时优先采用官方数据，网络或代理不可用时自动读取本地会话中的最近有效余量快照。
- 官方接口改为后台异步刷新，点击刷新后界面会先展示本地数据，不再因网络超时卡顿数秒。
- 官方接口与本地会话均无有效余量时显示“余量未知”，避免把数据不可用误显示为 0%。
- 保留本机今日 Token、缓存与输出统计，并在详情中明确标注当前数据来源。
- 延续优雅、简约、轻奢的紧凑与详细布局，以及随余量由灰绿向陶土红过渡的状态色。

## 数据说明

本地兜底数据来自本机 Codex 会话，只代表最近一次可见的周期余量快照。官方接口恢复后，应用会自动以官方数据覆盖本地快照。

## 下载

下载 `Remaining-Margin-Float-v1.2.0.exe`，并可使用同名 `.sha256` 文件核对完整性。

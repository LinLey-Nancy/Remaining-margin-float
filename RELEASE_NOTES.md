# Remaining Margin Float v1.8.6

## 额度数据稳定性

- Codex 官方接口成功返回后，会把该额度保存为当前最新数据；后续本地刷新仍更新
  Token 统计，但不会用落后的本地余量覆盖官方值。
- 网络超时、限流或暂时不可用时，继续显示最近成功的官方额度和真实采样时间，
  不再在官方值与本地快照之间来回跳动。
- 仅在本次运行尚未取得官方额度，或上一官方额度所属周期已经重置时，才使用
  当前本地余量快照。

## 快速下降口径

- “30 分钟内下降”现在按时间窗口起点样本到当前样本的净下降计算，不再取
  窗口内最高余量，因此中途短暂高点不会放大下降量。
- 关闭快速下降提醒后仍持续显示最近 1 小时的净下降量；关闭的只是 Windows
  通知，不再用“快速下降监控 · 已关闭”占用状态条。
- 快速下降会话至少保留 65 分钟连续样本，切换提醒开关后可以直接使用一小时口径。

## 趋势显示

- 24 小时与近 7 日趋势卡背景会跟随当前额度状态，在舒适、留意和低余量状态间
  使用对应的低饱和颜色。

## 验证与兼容

- 新增官方值优先、额度周期过期回退、窗口起点净下降、关闭提醒的一小时展示，
  以及诊断状态隔离回归。
- 保持 Windows PowerShell 5.1、现有设置、历史记录、提醒阈值和自动更新兼容。
- 安装程序继续使用相同 AppId，可直接覆盖升级 v1.8.5。

## 下载

下载 `Remaining-Margin-Float-v1.8.6-Setup.exe` 后运行安装向导，并可使用同名
`.sha256` 文件核对完整性。已启用自动更新的安装版，也可在网络条件合适时自动
完成下载、校验、覆盖安装和重启。

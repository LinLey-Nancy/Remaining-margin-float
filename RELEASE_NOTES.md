# Remaining Margin Float v1.8.1

## 启动同步提醒

- Codex 启动时先显示一次本地余量快照通知，明确标注数据来源、当前余量与快照时间。
- 官方接口连接成功后再显示一次官方同步通知，明确标注官方余量与采样时间。
- 本地旧快照与首次官方结果之间的变化不再触发快速下降提醒，避免把软件关闭期间或其他设备上的账号使用误判为本机持续消耗。
- 首次官方结果只用于建立监控基线；只有软件持续运行期间、同一数据通道的连续样本才参与快速下降判断。
- Provider 或数据通道切换、刷新长时间中断后会自动重建基线，官方缓存和等待官方同步期间的本地预览不参与快速下降判断。

## 详细布局修复

- 修复 400×560 详细布局中趋势图、下降量文字和快速下降状态条相互重叠的问题。
- 为趋势图保留稳定的最小高度，同时收紧账号区、指标区和统计区的纵向节奏，窗口尺寸保持不变。
- 优化四项指标卡的标题、数值和说明文字基线，并为中央分隔线保留独立留白，解决文字穿线和上下排拥挤。
- Codex 与 DeepSeek 两种数据形态均完成真实 WPF 截图检查。

## 验证与兼容

- 新增启动本地快照、首次官方同步、本地预览、连续官方下降和监控中断重建基线的回归诊断。
- 新增趋势内容与快速下降状态条最小间距检查。
- 保持 Windows PowerShell 5.1、现有使用历史、提醒阈值、Provider 设置和覆盖升级兼容。
- 安装程序继续使用相同 AppId，可直接覆盖升级 v1.8.0。

## 下载

下载 `Remaining-Margin-Float-v1.8.1-Setup.exe` 后运行安装向导。可使用同名
`.sha256` 文件核对下载完整性；应用内更新也会自动校验 SHA-256 和内嵌版本。

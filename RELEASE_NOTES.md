# Remaining Margin Float v1.4.2

## 刷新性能基线

- 新增真实 Codex 与 DeepSeek 本地日志的冷启动、热读取耗时诊断，后续优化可以用同一口径比较，不再依赖主观体感。
- 新增 8 MB 合成 Codex 会话日志回归测试，验证读取范围始终限制在文件尾部；即使旧日志很大，也不会因为尾部缺少 `token_count` 而回扫整个文件。
- 性能诊断只在开发或 CI 主动调用时执行，不改变正式应用的界面、刷新频率、数据来源或隐私行为。

## 架构与持续集成

- Provider 公共快照契约使用固定脱敏样例回归，Codex 与 DeepSeek 响应结构变化会在发布前被发现。
- Provider 价格常量集中到统一目录，减少同一价格在不同模块中不一致的风险。
- 刷新生命周期状态统一收敛到 `AppContext.Refresh`，降低忙碌状态、倒计时和请求状态彼此不同步的可能性。
- Windows 持续集成覆盖语法解析、应用诊断、透明发布目录构建与真实运行策略检查。
- GitHub Actions 的源码检出升级到基于 Node.js 24 的 `actions/checkout` v6.0.2，并固定到完整提交哈希。

## 兼容性

- 继续支持 Windows PowerShell 5.1、WPF 和零运行时第三方依赖。
- 用户界面、配置路径、60 秒自动刷新周期和启动方式保持不变，升级过程对日常使用无感。
- 发布包仍为未签名的透明便携包，包含可检查的合并脚本及其 SHA-256 信任边界。

## 验证

- 完整源码诊断覆盖 Provider 契约、刷新性能、DeepSeek 计费、历史趋势、窗口定位、贴边隐藏、启动、状态转换和刷新协调器。
- 发布检查覆盖组件合并、真实 WPF 回调、刷新计时器、已有实例唤醒、缺失或篡改脚本拒绝、启动入口选择以及最终 ZIP 内容。

## 下载

下载 `Remaining-Margin-Float-v1.4.2.zip`，解压后保持目录内容完整，并运行
`RemainingMarginFloat.exe`。可使用同名 `.sha256` 文件核对下载完整性。

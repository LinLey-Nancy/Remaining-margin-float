# Remaining Margin Float v1.6.0

## 跨重启使用历史

- 24 小时与近 7 日曲线现在使用稳定的本机历史文件，重启软件、更新版本后继续加载。
- 历史格式升级到 v2，同时兼容并自动读取已有 v1 JSONL 记录。
- 每条样本同时记录 UTC、当前本地日期、时区和 UTC 偏移；导入到另一台电脑后会按目标时区重新校准日期。
- 保留最近 8 个本地日历日，覆盖完整的近 7 日窗口；系统时钟短暂回拨时不会直接删除未来样本，但未来样本不会参与当前趋势和预测。
- 新命名目录迁移现在同时复制设置、DeepSeek 配置和使用历史。

## 记录迁移与脱敏诊断

- 悬浮窗和通知区域菜单新增“数据与诊断”。
- 支持导出、导入并合并脱敏的 `usage-history.jsonl`，便于换机、备份和恢复。
- 新增运行诊断窗口，可查看 Codex、DeepSeek、历史存储和刷新状态，复制或导出脱敏报告。
- 诊断报告会隐藏用户目录、邮箱、访问令牌、API Key 和疑似凭据，不包含原始会话日志。

## 紧凑布局

- 默认紧凑悬浮窗从 96×88 调整为 80×80 正方形。
- 同步收紧字号、内边距和进度条区域，并保留 370×500 详情窗口、拖动、贴边隐藏和多显示器边界处理。

## 验证与发布

- 新增重启重新加载、旧格式迁移、本地日期校准、导入合并、未来时钟偏差和诊断脱敏回归。
- 发布包仍只包含启动器、合并脚本、说明、许可证和隐私文件，不包含开发测试或仓库维护脚本。
- GitHub Release 标题从本版本起只显示版本标签，例如 `v1.6.0`。
- 继续支持 Windows PowerShell 5.1、WPF、60 秒刷新周期和零运行时第三方依赖。

## 下载

下载 `Remaining-Margin-Float-v1.6.0.zip`，解压后保持目录内容完整，并运行
`RemainingMarginFloat.exe`。可使用同名 `.sha256` 文件核对下载完整性。

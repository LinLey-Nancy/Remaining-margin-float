# Remaining Margin Float v1.12.2

## 改进

- 应用内部性能优化：长时间使用、历史记录累积较多时，自动刷新与趋势分析
  更流畅；界面倒计时与运行日志的固定开销进一步降低。
- 清理冗余代码、合并重复逻辑，程序更精简；功能、界面与使用习惯与上一
  版本完全一致。

## 下载

下载 `Remaining-Margin-Float-v1.12.2-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# Remaining Margin Float v1.12.1

## 修复

- 修复使用 Windows 桌面端 Kimi Code 时检测不到余量、一直显示「未知 · 5 小时
  余量未知」的问题：桌面端登录写入的托管凭证条目可能已过期，应用现在会在
  多个本地凭证中优先使用显式配置的凭证；自动凭证失效（401）时还会自动
  改用「Kimi Code 手动配置」中保存的 Key 立即重试，无需重新输入。
- 修复紧凑悬浮窗在余量未知时「5 小时余量未知」标签超出小窗被裁切的问题，
  标签现在会完整显示。

## 下载

下载 `Remaining-Margin-Float-v1.12.1-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# Remaining Margin Float v1.12.0

## 新增

- Kimi Code 数据源新增「在 WSL 中使用」开关：悬浮窗右键菜单与通知区域菜单各有一个可勾选项，仅在 Kimi Code 为当前数据源时显示。勾选后凭证（OAuth 令牌、`config.toml` 中的 API Key）与本地会话统计改从 WSL 发行版内的 `~/.kimi-code` 读取：应用枚举注册表中已安装的 WSL 发行版，通过 `\\wsl.localhost\<发行版>`（旧版 Windows 回退 `\\wsl$\<发行版>`）在 `root` 与各用户 `home` 目录下定位数据目录，解析结果按进程缓存，切换开关时重新解析。
- 勾选后在 WSL 里找不到 Kimi 数据时，界面显示「余量未知 / 暂无本地记录」，不会回退读取 Windows 侧的数据；账号行会注明凭证来源（Kimi Code CLI（WSL · OAuth 登录）/ Kimi Code CLI（WSL config.toml））。手动配置的 API Key 兜底逻辑两种模式下完全相同，`KIMI_CODE_HOME` 环境变量始终最优先（也可以直接指向 `\\wsl$` 路径，无需勾选开关）。
- 开关状态随设置持久化（`settings.json` 的 `KimiUseWsl` 字段），旧版本设置文件无此字段时按未勾选处理。

## 修复

- 修复安装、覆盖升级或卸载时后台进程残留导致「文件被占用、无法继续」的问题：应用在空闲时会启动一个无窗口的隐藏历史修复进程，安装程序的 Restart Manager 无法关闭它。现在安装与卸载开始前会按安装路径精确清理残留实例（无窗口后台进程立即终止，主窗口先优雅关闭、8 秒宽限后强制结束）；同时修复子进程在父进程退出后 3 秒内会自行退出，不再残留。便携版和其他目录的副本不受影响。
- 详情页「预计可用时间」对突然加速的消耗反应更快：在原有整段回归速度之外，并行计算最近 30 分钟的局部消耗速度，近段下降显著且更快时立即采用（例如 5 小时窗口快速消耗时，几分钟内就会从「30 天以上」更新为实际预测）；30 分钟内抖动不足 1pp 时维持原结论，不会因噪声频繁跳动。
- 修复 Windows PowerShell 5.1 下 Codex 会话解析里 `ConvertFrom-Json -Depth` 参数不存在导致的问题（该参数为 PowerShell 6 新增），改用默认深度，两个版本行为一致。

## 下载

下载 `Remaining-Margin-Float-v1.12.0-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# Remaining Margin Float v1.11.2

## 新增

- 使用提醒设置改为按数据源分开保存：Codex、Kimi Code、DeepSeek 各自一套低余量开关与阈值、快速下降开关与阈值，改动一个数据源不会影响其他数据源；设置对话框与右键、托盘菜单都跟随当前显示的数据源。
- 低余量提醒按配额窗口分别判断：Codex 与 Kimi Code 可以分别设置「5 小时余量低于」与「每周余量低于」两个阈值。DeepSeek 可以分别设置预算百分比阈值与账户余额阈值，其中金额阈值在未设置预算基准时同样有效。
- 快速下降提醒同样按窗口拆分：5 小时与每周各自一个下降阈值，状态行会标出是哪个窗口在下降（例如「检测到快速下降 · 5 小时 30 分钟内下降 12pp」）。
- 升级时旧设置文件里的低余量阈值与快速下降阈值会作为三个数据源的初始值，不会被重置。

## 修复

- 修复每周额度已经用完（0%）而 5 小时窗口刚好刷新到满格时，悬浮窗仍显示满格绿色额度的问题：此时头部改为显示每周额度，贴边能量条整条变灰，紧凑窗显示「0%」与「每周余量」，重置行跟随每周的重置时间；悬停提示里仍会给出 5 小时窗口当前的余量。
- 修复周额度归零时不会触发任何低余量提醒的问题：现在每个配额窗口按自己的阈值独立判断，也各自独立去重。

## 说明

- 头部改显示每周额度只是展示层切换：趋势图、快速下降样本与低余量提醒的作用域键仍然按套餐主额度隔离（Plus 与 Kimi Code 为 5 小时、Pro 为每周），历史不会被写入错误的周期。
- 提醒设置保存在 `settings.json` 的 `AlertSettings` 节点下，按 `Codex`、`Kimi`、`DeepSeek` 分组；旧版本的平铺字段会继续写入当前数据源的取值，便于回退到旧版本时仍能读取。

## 下载

下载 `Remaining-Margin-Float-v1.11.2-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# Remaining Margin Float v1.11.1

## 修复

- 修复使用一段时间后悬浮窗（含贴边能量条）完全消失、只能重启或双击托盘图标恢复的问题：贴边滑入/滑出动画被新动画打断时，旧的「动画完成后物理像素校准」仍可能在动画进行中采样屏幕边缘，把失真位置当作修正量一次性应用，将整个窗口推出屏幕；校准失败完全静默，出错后也没有任何机制复检，错误位置会一直保留到手动恢复。现改为动画进行中一律不执行校准（最新动画完成后一定会重新校准）；单次修正量超过 240 物理像素视为失真采样直接放弃；校准失败与失真采样都会写入运行日志。
- 新增窗口位置看门狗：刷新计时器每 5 秒检查一次窗口是否完全移出工作区，发现后自动重锚贴边位置或移回可见区域，并写入运行日志（托盘「数据与诊断」可查看），窗口无论因何种原因跑偏都能自动恢复。

## 下载

下载 `Remaining-Margin-Float-v1.11.1-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# Remaining Margin Float v1.11.0

## 新增

- DeepSeek 详情页上方两张卡片由「当前余额」「本月累计花费（本机日志估算）」改为「今日花费」与「本月花费」，数字来自 DeepSeek 官方余额接口的真实余额变化，不再使用本地日志按价格表估算。
- 充值、赠金到账只抬高计算基线，不计入花费，也不会冲抵已经花掉的钱。
- 首次启用时用现有 7 天趋势历史回填，本月花费立刻可用；覆盖不足整月时卡片副标题标注统计起点（例如「自 9/9 起统计」）。
- 应用没有运行时发生的余额下降，如果跨过了本地午夜，无法判断属于哪一天：同一个月内计入本月花费但不计入今日花费；跨月则不计入任何月份。这两种情况都会在卡片上标注「含断档」。
- 余额的精确金额从原来的卡片移到「余额构成」行标题，继续显示到分。

## 修复

- 修复重启后趋势样本数从几百骤降到几十甚至几个的问题：界面分析改用 5 分钟代表点（26 小时前为 30 分钟），并保留所有数值变化边界，磁盘上的原始采样始终完整未丢。
- 修复 Kimi Code 在 5 小时窗口空闲或刚重置时不显示 5 小时余量、退化为每周额度的问题：官方接口的 `limits[]` 只在窗口有活跃使用时返回，现回退到始终存在的 `usages.limit_5h` 摘要继续显示 5 小时余量与重置时间。
- 修复跨重启断档后「近 5 小时」趋势仍以重置前的旧值作为左端起点的问题：断档超过 10 分钟且当前余量高于最近记录（说明断档期间发生重置）时，趋势从当前余量重新开始绘制。

## 说明

- 花费台账保存在 `%LOCALAPPDATA%\RemainingMarginFloat\spend-ledger.json`，只保留当前月与上一个月，不包含账号名称、邮箱或任何凭据。
- 本机日志估算仍用于「今日 TOKEN」和「本月累计 TOKEN」，只是不再出现在花费卡片上。

## 下载

下载 `Remaining-Margin-Float-v1.11.0-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# Remaining Margin Float v1.10.2

## 改进

- 详情页左侧趋势卡由「24 小时」改为「近 5 小时」，与 5 小时额度窗口口径一致。
- 趋势曲线拉伸铺满整张卡片：最左侧是 5 小时内最早的样本，右边缘始终对应当前余量；采样间断用平滑曲线连接，额度重置（余量回升）处仍分段显示，不会把跳升画成平缓上升。
- 5 小时内没有样本时，以最近一次记录的样本作为起点，右边缘仍展示当前情况，卡片不再空白。
- 卡片标题、变化幅度和样本统计同步改为 5 小时口径。

## 下载

下载 `Remaining-Margin-Float-v1.10.2-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# v1.10.1

## 修复

- 修复额度重置后趋势段恰好只剩一个样本时，详情页提示“历史记录读取失败 / 历史记录暂不可用”、趋势与快速下降监控中断的问题；历史样本本身一直在正常记录，覆盖安装后趋势会立即恢复。
- 该问题在 Kimi Code 的 5 小时窗口重置后最容易出现，Codex 与 DeepSeek 的相同场景也一并修复。

# v1.10.0 内容（随 v1.10.1 一并发布）

## 新增 Kimi Code 数据源

- 新增第三个数据源 Kimi Code：零配置，应用直接读取 Kimi Code CLI 的本地凭据，无需在应用内填写任何密钥。
- 支持手动配置 Kimi API Key（右键菜单“Kimi Code 手动配置…”）：未安装 Kimi Code CLI、但把 Kimi 接入其他工具的用户也可查看配额；密钥经 Windows DPAPI 加密保存在本机，可随时清除，账号行会注明凭证来源。
- 通过官方配额接口展示双窗口额度：5 小时滚动窗口为主额度，每周配额为补充，紧凑窗直接显示 5 小时余量百分比。
- 从本地会话日志汇总今日 Token、最近一轮用量与缓存命中率。
- 趋势、低余量提醒和快速下降提醒对 Kimi Code 生效；快速下降阈值与 Codex 共用百分比点设置。
- 官方接口暂时失败时沿用上次成功数据并标明采样时间；数据过期后明确显示“余量未知”，不会伪装成 0%。

## 下载

下载 `Remaining-Margin-Float-v1.10.1-Setup.exe` 并运行安装向导，可用同名
`.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

---

# v1.9.1

## 更流畅、更稳定

- 大幅减少启动、刷新、展开和退出时的界面卡顿，长期使用并积累大量历史数据后仍能保持响应。
- 已有可用结果时，Codex 官方接口刷新不再先切换到本地会话；首次无历史数据时的本地结果也只作临时显示，避免详情布局闪动、自动展开和重复通知。
- 优化趋势记录和完整状态的读取、保存与后台整理，自动刷新不再长时间占用界面。
- 改善关闭流程，退出响应更快。

## 运行日志与问题诊断

- 新增本地脱敏运行日志，记录启动、刷新、窗口切换、历史保存、关闭耗时和异常，方便定位偶发问题。
- 日志会自动限制大小并轮转保存，同时隐藏凭据、邮箱和当前用户路径。
- 可从托盘菜单“数据与诊断”直接打开运行日志目录；诊断报告会附带最近的脱敏日志。

## 历史趋势兼容

- 修复部分 Pro / Pro Lite 用户覆盖安装或重新安装后旧的每周趋势不显示的问题。
- 旧趋势会按原有语义安全续接，并继续与 Plus 的 5 小时趋势隔离。

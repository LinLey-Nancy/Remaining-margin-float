# Remaining Margin Float v1.9.0

## Codex 5 小时与每周双周期额度

- 同时读取 Codex 的 5 小时额度与每周额度，并按照 300 分钟和 10080 分钟的
  实际窗口时长识别，不依赖接口把它们放在 primary 还是 secondary。
- 紧凑悬浮窗、贴边隐藏能量条和展开页顶部主指标统一显示 5 小时余额；每周额度
  不再替代 5 小时额度驱动主界面。
- 展开详情新增两条协调的额度带，分别展示 5 小时与每周的已用量、余额和各自的
  重置倒计时，同时保留今日 Token、输出、缓存和命中率摘要。

## 缺失数据与兼容行为

- 5 小时窗口缺失时明确显示“未知”，即使每周窗口仍然有效，也不会把每周数据
  冒充 5 小时数据；每周窗口缺失时同样保留清晰的未知状态。
- 官方接口与本地会话快照都支持双周期解析，旧的单窗口数据仍可读取，但只有
  明确的 5 小时窗口可以驱动 Codex 主余量。
- DeepSeek 的余额、预算百分比、Token 汇总和提醒逻辑保持不变。

## 趋势与提醒改为跟随 5 小时额度

- Codex 的 24 小时、近 7 日趋势、耗尽预测、低额度提醒和快速下降提醒全部改为
  使用 5 小时余额。
- 新趋势样本带有 `FiveHour` 周期标识；旧版本保存的每周百分比历史不会混入
  5 小时趋势，避免升级后出现曲线跳变或快速下降误报。
- 通知标题、提醒设置和状态文案同步注明“5 小时”，便于确认当前监控口径。

## 下载

正式发布后下载 `Remaining-Margin-Float-v1.9.0-Setup.exe` 并运行安装向导，可用
同名 `.sha256` 文件核对完整性。现有安装版可以直接覆盖升级，设置、历史状态和
开机启动方式会继续保留。

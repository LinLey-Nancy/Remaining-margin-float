<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Remaining Margin Float"
    Width="80"
    Height="80"
    MinWidth="80"
    MaxWidth="400"
    MinHeight="80"
    MaxHeight="560"
    WindowStyle="None"
    ResizeMode="NoResize"
    AllowsTransparency="True"
    Background="Transparent"
    Topmost="True"
    ShowInTaskbar="False"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True">
    <Window.Resources>
        <SolidColorBrush x:Key="TextPrimary" Color="#292D2A"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#626A65"/>
        <SolidColorBrush x:Key="TextMuted" Color="#8A918C"/>
        <SolidColorBrush x:Key="Sage" Color="#718478"/>
        <SolidColorBrush x:Key="SageSoft" Color="#EEF1ED"/>
        <SolidColorBrush x:Key="StatusStrong" Color="#46564C"/>
        <SolidColorBrush x:Key="StatusBorder" Color="#DCE3DD"/>
        <SolidColorBrush x:Key="Champagne" Color="#8A7658"/>
        <SolidColorBrush x:Key="Surface" Color="#FAFAF7"/>
        <SolidColorBrush x:Key="SurfaceSubtle" Color="#F4F5F1"/>
        <SolidColorBrush x:Key="Border" Color="#E1E3DE"/>
        <SolidColorBrush x:Key="Divider" Color="#E7E8E3"/>

        <Style x:Key="SoftButton" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="#F1F3EF"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="#E0E3DD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E9EEE9"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#C9D2CA"/>
                                <Setter Property="Foreground" Value="#46544A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.72"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#718478"/>
                                <Setter TargetName="ButtonBorder" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CompactHitStyle" TargetType="Border">
            <Setter Property="Background" Value="#01FFFFFF"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#718478"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MetricTitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="MetricValueText" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Display"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Typography.NumeralAlignment" Value="Tabular"/>
        </Style>

        <Style x:Key="MetricHintText" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,2,0,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="9.5"/>
        </Style>

        <Style x:Key="SectionTitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ChartMetaText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei UI"/>
            <Setter Property="FontSize" Value="9"/>
        </Style>
    </Window.Resources>

    <Grid x:Name="WindowRoot" Margin="5">
        <Border x:Name="HoverHalo"
                Margin="-1"
                CornerRadius="17"
                BorderThickness="1"
                BorderBrush="{DynamicResource Sage}"
                Background="Transparent"
                Opacity="0"/>

        <Border x:Name="Surface"
                Background="{StaticResource Surface}"
                BorderBrush="{StaticResource Border}"
                BorderThickness="1"
                CornerRadius="16"
                ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect x:Name="SurfaceShadow"
                                  Color="#58635B"
                                  BlurRadius="8"
                                  ShadowDepth="2"
                                  Direction="270"
                                  Opacity="0.07"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="69"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border x:Name="CompactHit"
                        Grid.Row="0"
                        Style="{StaticResource CompactHitStyle}"
                        Padding="7,5,7,5"
                        Cursor="Hand"
                        ToolTip="拖动移动 · 单击查看详情"
                        Focusable="True">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition x:Name="CompactProgressRow" Height="12"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,1,0,0">
                                <TextBlock x:Name="CompactPrefix"
                                           Text=""
                                           Margin="0,0,1,4"
                                           VerticalAlignment="Bottom"
                                           Foreground="{DynamicResource Sage}"
                                           FontFamily="Segoe UI Variable Text"
                                           FontSize="9"
                                           FontWeight="SemiBold"/>
                                <TextBlock x:Name="RemainingValue"
                                           Text="--"
                                           Foreground="{DynamicResource StatusStrong}"
                                           FontFamily="Segoe UI Variable Display"
                                           FontSize="23"
                                           FontWeight="SemiBold"
                                           FontStretch="SemiCondensed"
                                           LineHeight="26"
                                           Typography.NumeralAlignment="Tabular"/>
                                <TextBlock x:Name="CompactSuffix"
                                           Text="%"
                                           Margin="1,0,0,4"
                                           VerticalAlignment="Bottom"
                                           Foreground="{DynamicResource Sage}"
                                           FontFamily="Segoe UI Variable Text"
                                           FontSize="9"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock x:Name="WindowLabel"
                                       Margin="0,0,0,0"
                                       HorizontalAlignment="Center"
                                       Text="Codex 余量"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontFamily="Microsoft YaHei UI"
                                       FontSize="9"/>
                        </StackPanel>

                        <Grid Grid.Row="1" VerticalAlignment="Stretch">
                            <TextBlock x:Name="ExpandedWindowLabel"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Visibility="Collapsed"
                                       Text="Codex 余量"
                                       Foreground="{StaticResource TextMuted}"
                                       FontFamily="Microsoft YaHei UI"
                                       FontSize="8.5"
                                       FontWeight="SemiBold"/>
                            <StackPanel x:Name="ResetSummaryPanel"
                                        Orientation="Horizontal"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Visibility="Collapsed">
                                <TextBlock Text="下次重置 "
                                           Foreground="{StaticResource TextMuted}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="8.5"/>
                                <TextBlock x:Name="DetailsResetDate"
                                           Text="--"
                                           Foreground="{StaticResource TextSecondary}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="8.5"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text=" · "
                                           Foreground="{StaticResource TextMuted}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="8.5"/>
                                <TextBlock x:Name="DetailsResetCountdown"
                                           Text="--"
                                           Foreground="{StaticResource TextMuted}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="8.5"/>
                            </StackPanel>
                            <Border x:Name="ProgressTrack"
                                    Height="3"
                                    VerticalAlignment="Bottom"
                                    CornerRadius="1.5"
                                    Background="#DCDDD8"
                                    ClipToBounds="True"
                                    ToolTip="剩余 -- · 已使用 --">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Name="RemainingProgressColumn" Width="0*"/>
                                        <ColumnDefinition x:Name="UsedProgressColumn" Width="100*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{DynamicResource Sage}"/>
                                    <Border Grid.Column="1" Background="#DCDDD8"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>

                <Border x:Name="CompactDivider"
                        Grid.Row="1"
                        Background="{StaticResource Divider}"
                        Margin="16,0"/>

                <Grid x:Name="DetailsPanel"
                      Grid.Row="2"
                      Margin="20,16,20,16"
                      Visibility="Collapsed"
                      Opacity="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="54"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="142"/>
                        <RowDefinition Height="58"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="52"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <StackPanel VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="AccountName"
                                           Text="本地 Codex"
                                           Foreground="{StaticResource TextPrimary}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="15"
                                           FontWeight="SemiBold"/>
                                <Border Margin="9,1,0,0"
                                        Padding="7,2"
                                        VerticalAlignment="Center"
                                        Background="#F1EFEA"
                                        BorderBrush="#E1DDD3"
                                        BorderThickness="1"
                                        CornerRadius="5">
                                    <TextBlock x:Name="PlanBadge"
                                               Text="Codex"
                                               Foreground="#796B53"
                                               FontFamily="Segoe UI Variable Text"
                                               FontSize="9"
                                               FontWeight="SemiBold"/>
                                </Border>
                            </StackPanel>
                            <TextBlock x:Name="AccountEmail"
                                       Margin="0,5,0,0"
                                       Text="未找到账号信息"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontFamily="Segoe UI Variable Text"
                                       FontSize="10.5"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                        </StackPanel>

                        <Button x:Name="CloseButton"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Width="34"
                                Height="34"
                                Style="{StaticResource SoftButton}"
                                Padding="0"
                                ToolTip="收起详情"
                                AutomationProperties.Name="收起详情">
                            <TextBlock Text="&#xE70E;"
                                       Foreground="#69716C"
                                       FontFamily="Segoe Fluent Icons"
                                       FontSize="11"/>
                        </Button>
                    </Grid>

                    <Border Grid.Row="2"
                            Background="{DynamicResource SageSoft}"
                            CornerRadius="12"
                            Padding="14,9">
                        <Grid>
                            <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="1"/>
                            <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    Background="Transparent"
                                    BorderThickness="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock x:Name="MetricOneTitle" Text="已用额度" Style="{StaticResource MetricTitleText}"/>
                                    <TextBlock x:Name="PrimaryMetricValue"
                                               Text="--"
                                               Style="{StaticResource MetricValueText}"
                                               Foreground="{DynamicResource StatusStrong}"/>
                                    <TextBlock x:Name="PrimaryMetricHint"
                                               Text="剩余 --"
                                               Style="{StaticResource MetricHintText}"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock x:Name="MetricTwoTitle" Text="今日 TOKEN" Style="{StaticResource MetricTitleText}"/>
                                    <TextBlock x:Name="TodayTokens" Text="--" Style="{StaticResource MetricValueText}"/>
                                    <TextBlock x:Name="MetricTwoHint"
                                               Text="本机任务累计"
                                               Style="{StaticResource MetricHintText}"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                            </Border>

                            <Border Grid.Row="1"
                                    Height="1"
                                    Margin="0"
                                    Background="#DDE3DC"/>

                            <Border Grid.Row="2"
                                    Background="Transparent"
                                    BorderThickness="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="18"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock x:Name="MetricThreeTitle" Text="今日输入" Style="{StaticResource MetricTitleText}"/>
                                    <TextBlock x:Name="LastTurnTokens" Text="--" Style="{StaticResource MetricValueText}"/>
                                    <TextBlock x:Name="ContextText"
                                               Text="所有本机任务"
                                               Style="{StaticResource MetricHintText}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock x:Name="MetricFourTitle" Text="今日输出" Style="{StaticResource MetricTitleText}"/>
                                    <TextBlock x:Name="CacheHit" Text="--" Style="{StaticResource MetricValueText}"/>
                                    <TextBlock x:Name="CacheTokenText"
                                               Text="所有本机任务"
                                               Style="{StaticResource MetricHintText}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                                </StackPanel>
                            </Grid>
                            </Border>
                        </Grid>
                    </Border>

                    <Border Grid.Row="3"
                            Margin="0,8,0,0"
                            Background="Transparent"
                            BorderBrush="{StaticResource Divider}"
                            BorderThickness="0,1,0,0"
                            CornerRadius="0"
                            Padding="4,9,4,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock x:Name="BreakdownTitle" Text="今日缓存" Style="{StaticResource MetricTitleText}"/>
                                <TextBlock x:Name="TokenBreakdown"
                                           Margin="0,4,0,0"
                                           Text="-- cached  ·  命中 --"
                                           Foreground="{StaticResource TextPrimary}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="10.5"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <TextBlock x:Name="SecondaryMetricTitle"
                                           Text="额度状态"
                                           HorizontalAlignment="Right"
                                           Style="{StaticResource MetricTitleText}"/>
                                <TextBlock x:Name="ResetCount"
                                           Margin="0,4,0,0"
                                           Text="等待数据"
                                           HorizontalAlignment="Right"
                                           Foreground="{StaticResource TextPrimary}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="10.5"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="4" Margin="0,10,0,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="7"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="7"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <TextBlock Text="使用趋势"
                                       Style="{StaticResource SectionTitleText}"/>
                            <TextBlock x:Name="PredictionText"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       MaxWidth="250"
                                       Text="积累 30 分钟后预测"
                                       Foreground="{DynamicResource StatusStrong}"
                                       FontFamily="Microsoft YaHei UI"
                                       FontSize="9.5"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"/>
                        </Grid>

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Background="#F3F5F1"
                                    CornerRadius="9"
                                    Padding="10,7">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="17"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="13"/>
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <TextBlock Text="24 小时"
                                                   Foreground="{StaticResource TextSecondary}"
                                                   FontFamily="Microsoft YaHei UI"
                                                   FontSize="9.5"
                                                   FontWeight="SemiBold"/>
                                        <TextBlock x:Name="Trend24Text"
                                                   HorizontalAlignment="Right"
                                                   Text="积累中"
                                                   Foreground="{DynamicResource StatusStrong}"
                                                   FontFamily="Microsoft YaHei UI"
                                                   FontSize="10.5"
                                                   FontWeight="SemiBold"/>
                                    </Grid>
                                    <Canvas x:Name="Trend24Canvas"
                                            Grid.Row="1"
                                            Width="150"
                                            Height="43"
                                            HorizontalAlignment="Center"
                                            ClipToBounds="True">
                                        <Border Width="150" Height="1"
                                                Canvas.Top="21"
                                                Background="#DDE2DC"/>
                                        <Polygon x:Name="Trend24Area"
                                                 Fill="#244F6B58"/>
                                        <Polyline x:Name="Trend24Line"
                                                  Stroke="{DynamicResource StatusStrong}"
                                                  StrokeThickness="2"
                                                  StrokeLineJoin="Round"/>
                                        <Ellipse x:Name="Trend24StartMarker"
                                                 Width="4" Height="4"
                                                 Fill="#FFFFFF"
                                                 Stroke="{DynamicResource StatusStrong}"
                                                 StrokeThickness="1.5"/>
                                        <Ellipse x:Name="Trend24EndMarker"
                                                 Width="5" Height="5"
                                                 Fill="{DynamicResource StatusStrong}"/>
                                    </Canvas>
                                    <TextBlock x:Name="Trend24MetaText"
                                               Grid.Row="2"
                                               Text="等待更多样本"
                                               Style="{StaticResource ChartMetaText}"
                                               TextTrimming="CharacterEllipsis"/>
                                </Grid>
                            </Border>

                            <Border Grid.Column="2"
                                    Background="#F3F5F1"
                                    CornerRadius="9"
                                    Padding="10,7">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="17"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="13"/>
                                    </Grid.RowDefinitions>
                                    <Grid>
                                        <TextBlock Text="近 7 日"
                                                   Foreground="{StaticResource TextSecondary}"
                                                   FontFamily="Microsoft YaHei UI"
                                                   FontSize="9.5"
                                                   FontWeight="SemiBold"/>
                                        <TextBlock x:Name="Trend7Text"
                                                   HorizontalAlignment="Right"
                                                   Text="积累中"
                                                   Foreground="{DynamicResource StatusStrong}"
                                                   FontFamily="Microsoft YaHei UI"
                                                   FontSize="10.5"
                                                   FontWeight="SemiBold"/>
                                    </Grid>
                                    <Canvas x:Name="Trend7Canvas"
                                            Grid.Row="1"
                                            Width="150"
                                            Height="43"
                                            HorizontalAlignment="Center"
                                            ClipToBounds="True">
                                        <Border Width="150" Height="1"
                                                Canvas.Top="21"
                                                Background="#DDE2DC"/>
                                        <Polygon x:Name="Trend7Area"
                                                 Fill="#244F6B58"/>
                                        <Polyline x:Name="Trend7Line"
                                                  Stroke="{DynamicResource StatusStrong}"
                                                  StrokeThickness="2"
                                                  StrokeLineJoin="Round"/>
                                        <Ellipse x:Name="Trend7StartMarker"
                                                 Width="4" Height="4"
                                                 Fill="#FFFFFF"
                                                 Stroke="{DynamicResource StatusStrong}"
                                                 StrokeThickness="1.5"/>
                                        <Ellipse x:Name="Trend7EndMarker"
                                                 Width="5" Height="5"
                                                 Fill="{DynamicResource StatusStrong}"/>
                                    </Canvas>
                                    <TextBlock x:Name="Trend7MetaText"
                                               Grid.Row="2"
                                               Text="等待更多样本"
                                               Style="{StaticResource ChartMetaText}"
                                               TextTrimming="CharacterEllipsis"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <Border Grid.Row="4"
                                Background="#EEF2ED"
                                CornerRadius="7"
                                Padding="9,5">
                            <Grid>
                                <Ellipse x:Name="RapidDropStatusDot"
                                         Width="6"
                                         Height="6"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Center"
                                         Fill="{DynamicResource Sage}"/>
                                <TextBlock x:Name="RapidDropText"
                                           Margin="13,0,0,0"
                                           VerticalAlignment="Center"
                                           Text="快速下降监控 · 正在积累样本"
                                           Foreground="{StaticResource TextSecondary}"
                                           FontFamily="Microsoft YaHei UI"
                                           FontSize="9.5"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="14"/>
                            <RowDefinition Height="38"/>
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="SourceText"
                                       Margin="0,0,8,0"
                                       Text="Codex 本地会话快照"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontFamily="Microsoft YaHei UI"
                                       FontSize="9"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="SampleTime"
                                       Grid.Column="1"
                                       Text="采样于 --"
                                       Foreground="{StaticResource TextMuted}"
                                       FontFamily="Microsoft YaHei UI"
                                       FontSize="9"/>
                        </Grid>
                        <Grid Grid.Row="1">
                            <TextBlock x:Name="AutoRefreshText"
                                       VerticalAlignment="Center"
                                       Text="60 秒后自动刷新"
                                       Foreground="{StaticResource TextMuted}"
                                       FontFamily="Microsoft YaHei UI"
                                       FontSize="10"/>
                            <Button x:Name="RefreshButton"
                                    HorizontalAlignment="Right"
                                    Width="96"
                                    Style="{StaticResource SoftButton}"
                                    Content="立即刷新"
                                    ToolTip="重新读取当前数据源"
                                    AutomationProperties.Name="立即刷新"/>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </Border>

        <Grid x:Name="UltraCompactPanel"
              Panel.ZIndex="3"
              Width="14"
              Background="#0CFFFFFF"
              HorizontalAlignment="Right"
              VerticalAlignment="Stretch"
              SnapsToDevicePixels="True"
              UseLayoutRounding="True"
              IsHitTestVisible="False"
              Opacity="0">
            <Border x:Name="UltraProgressTrack"
                    Width="12"
                    Margin="0"
                    VerticalAlignment="Stretch"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    CornerRadius="2"
                    ClipToBounds="True"
                    SnapsToDevicePixels="True"
                    UseLayoutRounding="True"
                    ToolTip="剩余 --">
                <Grid>
                    <Grid Margin="1">
                        <Grid.RowDefinitions>
                            <RowDefinition x:Name="UltraEmptyProgressRow" Height="100*"/>
                            <RowDefinition x:Name="UltraRemainingProgressRow" Height="0*"/>
                        </Grid.RowDefinitions>
                        <Border x:Name="UltraProgressFill"
                                Grid.RowSpan="2"
                                Margin="0"
                                CornerRadius="1">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#25D982" Offset="0"/>
                                    <GradientStop Color="#76D955" Offset="0.42"/>
                                    <GradientStop Color="#FFD447" Offset="0.6"/>
                                    <GradientStop Color="#FF8A3D" Offset="0.8"/>
                                    <GradientStop Color="#F34F60" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                        <Border x:Name="UltraDepletedMask"
                                Grid.Row="0"
                                Background="#BCEFEFEA"/>
                        <Border x:Name="UltraLevelMarker"
                                Grid.Row="1"
                                Height="2"
                                VerticalAlignment="Top"
                                Background="#F2FFFFFF"/>
                    </Grid>
                    <Border x:Name="UltraProgressOutline"
                            Panel.ZIndex="2"
                            Background="Transparent"
                            BorderBrush="#F02B3831"
                            BorderThickness="1"
                            CornerRadius="2"
                            IsHitTestVisible="False"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
